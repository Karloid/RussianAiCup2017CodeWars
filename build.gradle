buildscript {
    repositories {
        jcenter()
    }
}

allprojects {
    apply plugin: "idea"

    version = '1.0'
    ext {
        appName = "RussianAiCup2017"
    }
}

project(":core") {
    apply plugin: "java"
}